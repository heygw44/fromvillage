spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    baseline-on-migrate: true
    baseline-version: 0

  session:
    store-type: redis
    timeout: 30m
    redis:
      repository-type: indexed
      namespace: fromvillage:session

  batch:
    job:
      enabled: false

server:
  servlet:
    session:
      cookie:
        http-only: true
        secure: true
        same-site: Lax
      tracking-modes: cookie

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
